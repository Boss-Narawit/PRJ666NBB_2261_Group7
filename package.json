{
  "name": "redrobe",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "install:all": "npm i && cd frontend && npm i && cd ../backend && npm i",
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "frontend/**/*.{ts,tsx,js,jsx}": [
      "eslint --config frontend/.eslintrc.js --resolve-plugins-relative-to frontend --no-ignore --fix",
      "prettier --write"
    ],
    "**/*.{json,yml,yaml,css,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "prettier": "^3.2.0"
  }
}
